<div class="navbar">
  <div class="container-nico navbar-nico">
    <div class='logo'>
      <%= link_to root_path do %>
        <i class="fas fa-cocktail"></i> Mr Cocktail
      <% end %>
    </div>
    <div class="search-bar">
      <%= simple_form_for :search, url: search_cocktails_path, method: :get do |f| %>
        <div class="search-form-control form-group">
          <input class="form-control string required" type="text" name="search" id="search_query" />
          <button name="button" type="submit" class="btn btn-flat">
            <i class="fas fa-search"></i> Search
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>
