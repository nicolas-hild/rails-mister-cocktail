<div class="form-nico">
  <%= simple_form_for [cocktail, dose] do |f| %>
    <%= f.input :quantity %>
    <%= f.input :mesure %>
    <%= f.association :ingredient %>
    <%= f.submit "Add ingredient", class: 'form-control' %>
  <% end %>
</div>
